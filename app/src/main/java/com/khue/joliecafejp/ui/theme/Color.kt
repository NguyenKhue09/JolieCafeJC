package com.khue.joliecafejp.ui.theme

import androidx.compose.material.Colors
import androidx.compose.ui.graphics.Color

val Purple200 = Color(0xFFBB86FC)
val Purple500 = Color(0xFF6200EE)
val Purple700 = Color(0xFF3700B3)
val Teal200 = Color(0xFF03DAC5)

private val grey_primary = Color(0xFF291B11)
private val grey_primary_variant = Color(0xFFBC834B)
private val grey_secondary = Color(0xFFCBC1B5)
private val text_color = Color(0xFFE4E4E4)
private val text_color_2 = Color(0xFFE7A15C)
private val dark_text_color = Color(0xFF707070)
private val grey_opacity60_primary = Color(0xFF746860)
private val title_text_color = Color(0xFFE7A15C)
private val button_text_color = Color(0xFF4E341B)
private val error_text_color = Color(0xFFF24E1E)

val Colors.errorTextColor
    get() = error_text_color

val Colors.buttonTextColor
    get() = button_text_color

val Colors.greyPrimary
    get() = grey_primary

val Colors.greyPrimaryVariant
    get() = grey_primary_variant

val Colors.greySecondary
    get() = grey_secondary

val Colors.textColor
    get() = text_color

val Colors.textColor2
    get() = text_color_2

val Colors.darkTextColor
    get() = dark_text_color

val Colors.greyOpacity60Primary
    get() = grey_opacity60_primary

val Colors.titleTextColor
    get() = title_text_color

